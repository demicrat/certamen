'use client';
import type { Settings, GameMode } from '@/lib/competition';
export default function GameSettings({ settings, onChange, disabled, mode }: { mode: GameMode; settings: Settings; onChange: (settings: Settings) => void; disabled?: boolean }) {
 return <fieldset disabled={disabled} className="game-settings"><legend className="eyebrow">MAKE IT YOUR MATCH</legend><label>Question pack<select value="practice" disabled><option value="practice">Classics warm-up · Original practice</option></select></label><div className="settings-row"><label>Questions<select value={settings.questions} onChange={e => onChange({ ...settings, questions: Number(e.target.value) })}>{[1, 5, 10, 12].map(n => <option key={n} value={n}>{n} questions</option>)}</select></label><label>Answer window<select value={settings.answerTime} onChange={e => onChange({ ...settings, answerTime: Number(e.target.value) })}>{[5, 10, 15, 20, 30].map(n => <option key={n} value={n}>{n} seconds</option>)}</select></label><label>Reading pace<select value={settings.wordMs} onChange={e => onChange({ ...settings, wordMs: Number(e.target.value) })}><option value={500}>Relaxed</option><option value={340}>Standard</option><option value={220}>Speed round</option></select></label></div><p className="small-note">10 points per tossup, 5 per correct bonus. Question count selects tossup sets. No penalties. {mode === 'teams' ? 'One buzz locks out the entire team for that question, right or wrong. Bonuses belong to the team that answers the tossup correctly. The moderator judges typed or spoken answers.' : mode === 'solo' ? 'Submit your answer, then check it yourself.' : 'Bonuses are open to everyone as 5-point tossups. One attempt per player. Everyone reviews the answer together; unanimous agreement awards points.'}</p></fieldset>;
}
